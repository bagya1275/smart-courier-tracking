/*
 * Deployment configuration.
 *
 * For a single-service deployment (recommended), leave this empty and the
 * frontend will call /api/v1 on the same host.
 *
 * If the frontend is hosted separately, set this to the public backend URL,
 * for example: https://smart-courier-api.example.com/api/v1
 */
window.SMART_COURIER_API_BASE_URL = '';
